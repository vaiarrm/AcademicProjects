###author: Vaibhav Sharma#Date: 10/22/2016#Description: Calculates the first order derivative of softmax transfer function for Z#Output: Returns the value of first order derivative of softmax function##function softDerRet = sigmoid_prime(z,currLayer)  global numberOfLayers    if currLayer == numberOfLayers     soft = softmax(z,currLayer);     softDerRet = soft .* (1 - soft);  else    softDerRet = sigmoid_prime(z,currLayer)  ;  endif   endfunction